# 0.6.14 Bundle — CEO Briefing

**Drafted**: 2026-05-20 by Advisor
**Target**: ONE codex CEO session, final bundle work
**Authoritative**: this file

---

## Pre-state (already on main, ready to bundle)

3 feature commits cherry-picked onto `~/Projects/AiPlus/aiplus-public` main:

```
7c8339f feat(cost): include local session token coverage
f4a9f9b feat(freshen): detect stale MCP and refresh personas
25b624b fix(installer): smoke test installed binary
```

Branch HEAD is at `7c8339f`, ahead of origin/main by 3 commits, not pushed. Cargo.toml version is `0.6.13`, install.sh fallback is `v0.6.13` — both need bump.

## Scope

Tiny scope. Bundle prep only. No new code.

### Phase 1 — Version bump (~5 min)

1. `~/Projects/AiPlus/aiplus-public/crates/aiplus-cli/Cargo.toml`: bump `version = "0.6.13"` → `version = "0.6.14"` (one line).
2. `~/Projects/AiPlus/aiplus-public/install.sh`: bump `VERSION="${VERSION:-v0.6.13}"` → `VERSION="${VERSION:-v0.6.14}"` (one line, the comment says "fallback if both lookups fail (last-known-good)").
3. `cargo build -p aiplus-cli` to refresh `Cargo.lock`.
4. Verify `./target/debug/aiplus --version` prints `0.6.14`.

### Phase 2 — CHANGELOG 0.6.14 entry (~10 min)

`~/Projects/AiPlus/aiplus-public/CHANGELOG.md` — INSERT new `## 0.6.14` section ABOVE the existing `## 0.6.13` section. Use this content verbatim:

```markdown
## 0.6.14

### Token cost coverage: session + dispatch (G-AT-COST-COVERAGE-1)

`agent_token_cost` MCP tool + `aiplus agent token-cost` CLI now report
**both AiPlus dispatch tokens AND local codex session tokens** by
default. Previously the tool only counted aiplus agent dispatch
overhead — for Owner who burns most tokens in the codex main session,
the answer was always ~0 and never matched the actual question.

New fields per window:

| Field | Source |
|---|---|
| `dispatch_tokens` / `dispatch_usd` | AiPlus dispatch log |
| `session_tokens` / `session_usd` | Local codex `~/.codex/state_5.sqlite` (READ-ONLY) |
| `total_tokens` / `total_usd` | Sum of above |

New CLI flags:

```bash
aiplus agent token-cost --window 168h         # weekly, default both
aiplus agent token-cost --session-only        # codex sessions only
aiplus agent token-cost --dispatch-only       # aiplus dispatch only
```

New windows: `7d` / `168h` for weekly rollups.

SKILL.md + tool description add Chinese trigger examples ("本周烧了多少 token",
"最近花了多少") so Discovery layer routes natural-language queries correctly.

**Caveat**: codex's `threads.tokens_used` sqlite field is total tokens only —
not input/output split — so `session_usd` is **estimated** by pricing all
tokens at the model's input-token rate. Real cost is somewhat less. Order
of magnitude is correct.

Claude Code session rollup deferred to a future goal; codex session
support ships now.

### Project freshness: mcp-register update + persona refresh + doctor warnings (G-AT-FRESHEN-1)

After MailCue dogfood revealed that the codex MCP server pointer
silently stayed on an old vendored binary while the installed `aiplus`
binary upgraded — defeating two weeks of Discovery v2 work without
warning — this release adds freshness detection + repair tooling.

**`aiplus mcp-register --runtime <r>`** now detects an existing config
entry pointing to a different binary path, prompts to update (TTY) or
auto-updates with notice (non-TTY).

**New command `aiplus persona refresh [--dry-run] [--yes]`**:
walks `.aiplus/agents/personas/*.md` + `.aiplus/AGENTS.aiplus.md`,
compares each AiPlus Tool Discovery preamble block to canonical
templates, and replaces ONLY the preamble — preserving project
customizations in the persona body.

**`aiplus doctor`** adds two INFO-severity warnings:
- `DOCTOR_WARN_MCP_BINARY_STALE` — configured MCP binary path differs from current `aiplus`
- `DOCTOR_WARN_PERSONA_DRIFT` — persona file missing or has outdated discovery preamble

### Install smoke test (G-AT-INSTALL-SMOKE-1)

After today's `zsh: killed aiplus` macOS 26.4 SIGKILL incident where
install.sh printed PASS but the installed binary was unrunnable,
`install.sh` now runs a post-install smoke test (runs `aiplus --version`
after copy). On non-zero exit: `SMOKE_FAIL=exit=<code>`, `INSTALL_STATUS=FAIL`,
clear remediation hints, `exit 1`. Catches platform-specific install
failures at install time instead of first-run time.

### Bundled commits

- 25b624b — install.sh smoke test
- f4a9f9b — freshen (mcp-register + persona refresh + doctor warns)
- 7c8339f — cost-coverage (session tokens + Chinese examples + new flags)

```

### Phase 3 — Commit (~2 min)

Stage all 3 modified files (Cargo.toml, Cargo.lock, install.sh, CHANGELOG.md). Single commit:

```
chore(release): aiplus-cli 0.6.14 — install smoke + freshen + cost coverage / aiplus-cli 0.6.14 — 安装冒烟 + 项目刷新 + token 覆盖
```

Body should briefly note the 3 bundled goals. Use HEREDOC format with `Co-Authored-By: Codex CEO`.

### Phase 4 — Evidence

- `cargo build -p aiplus-cli` PASS
- `cargo test --workspace` PASS (expect 603+ tests)
- `cargo clippy --workspace --all-targets -- -D warnings` clean
- `./target/debug/aiplus --version` prints `0.6.14`
- `sh -n install.sh` PASS
- `git status` clean (no untracked, no unstaged) after commit
- `git log --oneline origin/main..main` shows 4 commits (3 features + 1 release)

## STOP rules (CRITICAL — per auto-memory `parallel_ceo_tag_gate.md`)

- `_REGRESSION_` → STOP
- `_TAG-BREACH_` → STOP (NEVER tag, NEVER push tag, NEVER push origin main)
- `_SCOPE-BREACH_` → STOP (touched forbidden file)
- Retry-once gate standard

**You are explicitly forbidden from**:
- `git tag` (any tag command)
- `git push origin main` (push to origin main)
- `git push --tags` or any tag push
- Modifying any source code (`crates/`, `assets/`) — your scope is ONLY Cargo.toml/install.sh/CHANGELOG.md/Cargo.lock
- Touching the 3 feature commits already on main (they're frozen, work ON TOP)

Branch push is fine if you create one (you don't need to). Advisor will Phase C ratify + ask Owner for tag.

## Time ceiling

Hard cap T+30min. Realistic ~15min.

## Deliverables (2)

1. 4 modified files (Cargo.toml, Cargo.lock, install.sh, CHANGELOG.md), single commit on main
2. Brief impl-notes at `docs/proposals/0.6.14-bundle-notes.md` (Phase 4 evidence + any deviations)

## Handoff

Report `IMPL_VERDICT={PASS | PASS_WITH_DEVIATIONS | BLOCKED}`. Advisor will:
- Verify build + version
- Write retrospective in aiplus-agent-team
- Push aiplus-public main
- Ask Owner for tag authorization

---

— Advisor, 2026-05-20. Final bundle for v0.6.14. ~15min scope. NO TAG, NO PUSH.
